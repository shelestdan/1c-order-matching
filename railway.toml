[build]
builder = "nixpacks"

[deploy]
startCommand = "cd scripts && python -m uvicorn matching_api:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/api/health"
healthcheckTimeout = 60
restartPolicyType = "on_failure"

[environment]
NIXPACKS_PYTHON_VERSION = "3.11"
